# @package _global_
defaults:
  - override /model: Llama-3.2-1B-Instruct
  - override /data/datasets@data.forget: Clinical_forget
  - override /data/datasets@data.retain: Clinical_retain
  - override /eval: tofu
mode: eval
forget_split: forget
retain_split: retain
retain_logs_path: null
eval:
  tofu:
    forget_split: ${forget_split}
    retain_logs_path: ${retain_logs_path}
    overwrite: true
task_name: ???
