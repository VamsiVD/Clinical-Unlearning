# @package _global_
defaults:
  - override /model: Llama-3.2-1B-Instruct
  - override /trainer: GradDiff
  - override /data/datasets@data.forget: Clinical_forget
  - override /data/datasets@data.retain: Clinical_retain
mode: unlearn
forget_split: forget
retain_split: retain
task_name: ???
model:
  model_args:
    pretrained_model_name_or_path: saves/finetune/clinical_finetuned_v2
trainer:
  args:
    learning_rate: 1e-5
    num_train_epochs: 5
    per_device_train_batch_size: 4
    gradient_accumulation_steps: 4
