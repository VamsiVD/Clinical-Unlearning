# @package _global_
defaults:
  - override /model: Llama-3.2-1B-Instruct
  - override /trainer: finetune
  - override /data/datasets@data.train: Clinical_forget
mode: finetune
task_name: ???
trainer:
  args:
    learning_rate: 1e-5
    num_train_epochs: 10
    per_device_train_batch_size: 4
    gradient_accumulation_steps: 4
